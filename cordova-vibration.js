// Generated by CoffeeScript 1.10.0
(function() {
  Polymer({
    is: "cordova-vibration",
    properties: {

      /* If true, automatically performs vibration when device is ready. */
      auto: {
        reflectToAttribute: true,
        type: Boolean,
        value: false
      },

      /* Return if cordova deviceready event has been fired. */
      ready: {
        notify: true,
        observer: "_observeReady",
        readOnly: true,
        reflectToAttribute: true,
        type: Boolean,
        value: false
      },

      /* Milliseconds to vibrate the device. */
      time: {
        reflectToAttribute: true,
        type: Number,
        value: 1000
      }
    },
    _observeReady: function() {
      if (this.auto) {
        return this.vibrate();
      }
    },

    /* Vibrates the device for a given amount of time. */
    vibrate: function() {
      if (this.ready) {
        return navigator.vibrate(this.time);
      }
    }
  });

}).call(this);

//# sourceMappingURL=cordova-vibration.js.map
